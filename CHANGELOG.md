# 更新公告

本文件记录每个版本的**用户可见变化**。技术细节（踩过什么坑、为什么这么改）在每次提交信息
与 [`README.md`](./README.md#工作原理) 里；这里只讲"你会看到什么不一样"。

---

## v0.2.0 —— 一键汉化

> 一句话：**选游戏 → 点一下 → 游戏已经是中文。**

### 新增：拖进去就认

界面上写着「拖进来，点一下，能读了」—— 这一版让它名副其实：
把 **`Game.exe`（或游戏文件夹）直接拖进窗口**就会选中该游戏并立刻识别引擎。
（拖 `Game.exe` 时取它**所在目录**；拖文件夹时直接用。判断在主进程做，渲染层拿不到可靠的文件类型。）

### 新增：一键汉化并启动（运行时）

界面汉化页新增一颗 **「一键汉化并启动」**，命令行对应 `--runtime "游戏目录"`。

点下去之后：程序把"运行时桥"装进游戏 → 启动游戏 → **在游戏运行中逐帧取词并翻成中文**。
关掉游戏，游戏文件**自动逐字节还原**（按 SHA-256 校验，改前备份）。

- **不改游戏数据文件。** 全部改动只有两处：`plugins.js` 里加一行注册、放一个桥插件；退出即还原。
- **数据被加密的游戏也能用。** 静态路线读不出被加密的数据，运行时**根本不读它** ——
  这一点对"打开后一片日文乱码/读不出文本"的游戏是关键。
- **边玩边翻，不用等整局翻完。** 界面上实时显示「取词 N 条 / 本地命中 N / 接口得译文 N」。
- **翻译不重复花钱。** 命中的句子直接走本地**译文库**；没命中才调你配置的翻译接口，结果回写译文库。
- **能直接用你已有的译文。** 如果游戏目录里已经有 `{原文: 译文}` 形式的中文字典，
  程序会**自动导入**当预置译文（实测某款真实 MV 游戏因此直接拿到 **59,847 条**现成译文）。

实测同一个真实游戏"什么时候能看到中文"：

| 场景 | 耗时 |
|---|---|
| 首次（要调接口） | 约 219 秒（其中绝大多数是这个游戏自身加载慢：3.4 GB 资源 / 140+ 插件） |
| 第二次（命中译文库） | **6 秒** |

> 目前支持 **RPG Maker MV / MZ**。其它引擎请先用下面的"静态汉化"。

### 保留：静态汉化（改游戏数据文件）

译文写进游戏数据文件 —— **一次改完，之后离线可玩、不依赖本程序运行**，
想改回来随时「一键还原」。两条路**共用同一套翻译能力**（同一个接口 + 同一份译文库），
不是两套系统。

### 修掉的问题（你会感知到的）

- **文字迟迟不变中文**：译文已经拿到，但游戏在标题界面这类静态画面上长时间不重绘 →
  现在**译文一到就主动刷新文本窗口**，当场生效。
- **界面点了按钮、命令行又跑一遍**会互相踩（两边同时往同一个游戏装/还原插件）→
  现在有**跨进程游戏目录锁**：第二个会明确告知"这个游戏已经在汉化中"，而不是默默改坏。
- 打包后的程序会把 `--runtime` 当成普通参数忽略 → 已修。

### 仍然没有的

- 应用图标、代码签名（首次运行会有 SmartScreen 提示，选"仍要运行"）。
- Unity 适配（资源容器解析已就绪，对象枚举未完成）。

---

## v0.1.0 —— 首个公开版本

### 能做什么

对 **RPG Maker MV / MZ / Ren'Py** 的真实游戏完成
「导入 → 汉化 → 修订 → 回写 → 还原」全流程：

- **一键汉化（静态）**：选目录 → 确认选项 → 开始。抽取 → 入库 → 查翻译记忆 → 翻译 → 回写 → 注入字体。
- **控制符保护**：`\C[1]` `\V[3]` 这类控制符翻译前扣出占位、翻完装回；**装不回去的拒绝写回**，
  而不是带着错误写进游戏。
- **人工修订工作台**：按文件浏览 / 搜索 / 状态筛选 / 单条编辑 / 批量替换（先给样例预演）/ 导入导出。
- **一键还原**：所有改动自动备份，还原后**逐字节一致**。
- **本地优先**：翻译记忆、术语表、文本库全在本机；除你自己配置的翻译接口外没有任何数据外发。

### 实测

- RPG Maker MV 真实游戏抽出 **4064** 条文本、MZ **7487** 条，控制符零损失。
- 回写幂等（连跑两次，第二次 `写入 0`）、还原逐字节一致。
- 免安装版 76.7 MB，双击即用。

### 已知限制

- 没有应用图标与代码签名。
- Unity 适配未完成（资源解析就绪，对象枚举待做）。
- KiriKiri 无可用真样本、无法验证，已移出计划。
